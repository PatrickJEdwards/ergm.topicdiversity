% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/InitErgmTerm.b1topicdiversity.R
\name{b1topicdiversity-ergmTerm}
\alias{b1topicdiversity-ergmTerm}
\alias{b1topicdiversity}
\title{Inverse Simpson Effective Topic Diversity of Mode-1 Neighborhoods}
\usage{
# binary: b1topicdiversity(topics, subtract.one = FALSE)
}
\arguments{
\item{topics}{A mode-2 vertex-attribute specification resolving to a
numeric matrix. Rows must correspond to mode-2 vertices and columns to
topics. In practice, this can be a character vector containing the names
of topic-proportion vertex attributes or a formula such as
\code{~ cbind(topic_1, topic_2, topic_3)}.}

\item{subtract.one}{Logical. If \code{FALSE} (the default), a nonisolated actor
contributes the ordinary inverse Simpson effective topic count D, while
an isolate contributes zero. If \code{TRUE}, a nonisolated actor contributes
D - 1. This centers a perfectly one-topic portfolio at zero and removes
the unavoidable baseline value of one from every nonisolate. A single
mixed-topic EDM can still contribute more than zero under this shifted
version.}
}
\description{
This binary bipartite ERGM term adds one statistic equal to the sum,
across first-mode actors, of the inverse Simpson effective number of topics
represented among the second-mode vertices to which each actor is tied.

For actor i, let theta\link{j,k} denote mode-2 vertex j's proportion on topic k.
The actor's accumulated topic mass is s\link{i,k} = sum_j y\link{i,j} theta\link{j,k}.
After normalizing by actor degree d\link{i}, its portfolio proportions are
p\link{i,k} = s\link{i,k} / d\link{i}. The inverse Simpson effective number of topics is
1 / sum_k p\link{i,k}^2.
}
\details{
Topic rows are normalized internally to sum to one. Therefore, all supplied
topic attributes must be finite and nonnegative, and every mode-2 vertex
must have positive total topic mass.

The term is dyad-dependent because the change associated with an MP-EDM tie
depends on the MP's existing topic portfolio.

The inverse Simpson effective number is mathematically identical to the
Laakso-Taagepera effective-number formula commonly used for political
parties, with topic proportions replacing party vote or seat shares.
}
\concept{binary}
\concept{bipartite}
\concept{undirected}
